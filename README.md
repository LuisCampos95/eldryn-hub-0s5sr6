# Painel (espelho público)

Só a **vitrine**. Todo o código, os dados e as credenciais ficam em um
repositório privado.

- `/` painel da Redação (funil, grade agendada, redes, gasto de IA)
- `/arquivo/` painel antigo de cards, mantido como consulta

Este repositório existe porque o GitHub Pages, no plano gratuito, só publica
de repositório público. Em vez de deixar o projeto inteiro exposto, aqui vai
apenas o que a página precisa carregar.

Atualizado automaticamente por `redacao/painel/publicar.js` do repositório
privado.
